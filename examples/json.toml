[inventory]
path = "inventory.json"
format = "json"
records_path = "assets"

[inventory.columns]
asset_id = "asset_id"
vendor = "vendor"
product = "product"
version = "version"

[state]
database = "../.cvebeacon/state.db"

[output]
directory = "../reports"
